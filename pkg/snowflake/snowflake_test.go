package snowflake

import (
	"testing"
)

func TestNewSnowflake(t *testing.T) {
	// snowflake := NewSnowflake("687bdd694b")
	// m := make(map[int64]struct{})
	// for {
	// 	go func() {
	// 		defer wg.Unlock()
	// 		wg.Lock()
	// 		id := snowflake.NextId()
	// 		if _, ok := m[id]; ok {
	// 			t.Log("重复了")
	// 		} else {
	// 			m[id] = struct{}{}
	// 		}
	// 		t.Log(id)
	// 	}()
	// }
}
